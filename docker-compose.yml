services:
  web:
    image: nginx
    volumes:
      - ./conf/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./conf/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./cert.crt:/etc/ssl/cert.crt:ro
      - ./cert.key:/etc/ssl/cert.key:ro
      - ./src:/var/www
      - ./node_modules:/var/www/modules
    ports:
      - "80:80"
      - "443:443"